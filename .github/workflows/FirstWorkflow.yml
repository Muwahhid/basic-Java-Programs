name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: windows-latest
    steps:
      - name: Print Greeting
        run: echo "Hello World!"
      - name: Print Done
        run: echo "Done-GoodBye!"
