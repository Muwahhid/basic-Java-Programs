name: First Workflow
on: workflow_dispatch
jobs:
  first-job
    runs-on: windows-latest
    steps:
      -name: Print Greeting
        echo "Hello World!"
      -name: Print Done
        echo "Done-GoodBye!"
